const image = (state = null, action) => {

    // Default value
    state = state || {
        key: null,
        url: null,
    }

    switch (action.type) {
        default:
            return state
    }
}

export default image;

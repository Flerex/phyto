const canvas = (state = null, action) => {

    // Default value
    state = state || {
        width: null,
        height: null,
    }

    switch (action.type) {
        default:
            return state
    }
}

export default canvas;
